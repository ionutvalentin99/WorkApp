<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm sticky-top py-1">
    <div class="container-xl position-relative d-flex justify-content-between align-items-center">

        {# 1. TOGGLER MOBIL STÂNGA (Meniul Principal) - VIZIBIL DOAR PENTRU LOGAȚI #}
        {% if app.user %}
            <button class="navbar-toggler border-0 p-2 d-lg-none modern-hover rounded-circle" type="button" data-bs-toggle="offcanvas"
                    data-bs-target="#mobileMainMenu" aria-controls="mobileMainMenu" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        {% else %}
            {# Placeholder invizibil pentru a menține alinierea butonului de cont în dreapta #}
            <div class="d-lg-none" style="width: 40px;"></div>
        {% endif %}

        {# 2. LOGO CENTRAT (Doar pe mobil) #}
        <a class="navbar-brand d-lg-none position-absolute start-50 translate-middle-x m-0" href="{{ path('app_home') }}">
            <img src="{{ asset('assets/client/images/epontaje.jpg') }}" alt="Logo" height="32" class="object-fit-contain">
        </a>

        {# 3. LOGO NORMAL (Desktop) #}
        <a class="navbar-brand d-none d-lg-flex align-items-center me-4" href="{{ path('app_home') }}">
            <img src="{{ asset('assets/client/images/epontaje.jpg') }}" alt="Logo" height="32" class="me-2 object-fit-contain">
        </a>

        {# 4. TOGGLER MOBIL DREAPTA (Contul Utilizatorului) #}
        <button class="navbar-toggler border-0 p-1 d-lg-none rounded-circle modern-hover" type="button" data-bs-toggle="offcanvas"
                data-bs-target="#mobileUserMenu" aria-controls="mobileUserMenu" aria-label="Toggle user menu">
            {% if app.user %}
                <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center fw-bold shadow-sm"
                     style="width: 36px; height: 36px; font-size: 14px;">
                    {{ app.user.firstName|first|upper }}{{ app.user.lastName ? app.user.lastName|first|upper : '' }}
                </div>
            {% else %}
                <i class="bi bi-person-circle fs-2 text-secondary"></i>
            {% endif %}
        </button>

        {# ========================================== #}
        {# ZONA OFF CANVAS - MENIU STÂNGA (PRINCIPAL) #}
        {# DOAR DACA ESTE LOGAT                       #}
        {# ========================================== #}
        {% if app.user %}
            <div class="offcanvas-lg offcanvas-start flex-grow-1 border-0" tabindex="-1" id="mobileMainMenu" aria-labelledby="mobileMainMenuLabel">
                <div class="offcanvas-header border-bottom d-lg-none">
                    <h5 class="offcanvas-title fw-bold" id="mobileMainMenuLabel">Meniu</h5>
                    <button type="button" class="btn-close shadow-none" data-bs-dismiss="offcanvas" data-bs-target="#mobileMainMenu" aria-label="Close"></button>
                </div>

                <div class="offcanvas-body pb-3 pb-lg-0">
                    {% set current_route = app.request.attributes.get('_route') %}
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-1 fw-medium">
                        <li class="nav-item">
                            <a class="nav-link px-3 py-2 rounded-pill transition-all {{ current_route == 'app_home' ? 'bg-primary bg-opacity-10 text-primary fw-bold' : 'text-secondary modern-hover' }}"
                               href="{{ path('app_home') }}">Dashboard</a>
                        </li>
                        {% if app.user.company is not null %}
                            <li class="nav-item">
                                <a class="nav-link px-3 py-2 rounded-pill transition-all {{ 'pontaj' in current_route ? 'bg-primary bg-opacity-10 text-primary fw-bold' : 'text-secondary modern-hover' }}"
                                   href="{{ path('app_pontaj') }}">Work Records</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link px-3 py-2 rounded-pill transition-all {{ 'concediu' in current_route ? 'bg-primary bg-opacity-10 text-primary fw-bold' : 'text-secondary modern-hover' }}"
                                   href="{{ path('app_concediu') }}">Holidays</a>
                            </li>
                        {% endif %}
                        {# Admin Nav #}
{#                        {% if is_granted("ROLE_ADMIN") %}#}
{#                            <li class="nav-item">#}
{#                                <a class="nav-link px-3 py-2 rounded-pill transition-all {{ 'crud' in current_route ? 'bg-primary bg-opacity-10 text-primary fw-bold' : 'text-secondary modern-hover' }}"#}
{#                                   href="{{ path('app_user_crud_index') }}">Admin</a>#}
{#                            </li>#}
{#                        {% endif %}#}
                    </ul>
                </div>
            </div>
        {% endif %}

        {# ========================================== #}
        {# ZONA OFF CANVAS - MENIU DREAPTA (CONT)     #}
        {# ========================================== #}
        <div class="offcanvas-lg offcanvas-end border-0" tabindex="-1" id="mobileUserMenu" aria-labelledby="mobileUserMenuLabel">
            <div class="offcanvas-header border-bottom d-lg-none">
                <h5 class="offcanvas-title fw-bold" id="mobileUserMenuLabel">Contul Meu</h5>
                <button type="button" class="btn-close shadow-none" data-bs-dismiss="offcanvas" data-bs-target="#mobileUserMenu" aria-label="Close"></button>
            </div>

            <div class="offcanvas-body">
                <div class="d-flex flex-column flex-lg-row align-items-lg-center gap-3 ms-auto w-100">

                    {% if not app.user %}
                        {# Nelogat Desktop & Mobile #}
                        <div class="d-flex flex-column flex-lg-row align-items-lg-center gap-2 w-100 mt-3 mt-lg-0">
                            <a href="{{ path('app_login') }}" class="btn text-decoration-none text-secondary fw-bold px-3 py-2 rounded-pill modern-hover">Sign in</a>
                            <a href="{{ path('app_registration') }}" class="btn btn-primary rounded-pill px-4 py-2 fw-medium shadow-sm transition-all text-white">Get Started</a>
                        </div>
                    {% else %}
                        {# ----------------- DESKTOP ACCOUNT VIEW ----------------- #}
                        <div class="d-none d-lg-flex align-items-center gap-3 ms-auto">
                            {% if not app.user.isVerified %}
                                <a href="{{ path('app_send_email_verification') }}"
                                   class="btn btn-warning btn-sm rounded-circle text-white shadow-sm"
                                   style="width: 36px; height: 36px; display:grid; place-items:center;" data-bs-toggle="tooltip"
                                   title="Confirm email"><i class="bi bi-exclamation-triangle-fill"></i></a>
                            {% endif %}

                            <div class="dropdown dropdown-hover">
                                <button class="btn bg-light modern-hover border-0 rounded-pill dropdown-toggle d-flex align-items-center gap-2 px-3 py-2 transition-all"
                                        type="button" data-bs-toggle="dropdown" aria-expanded="false" style="outline: none; box-shadow: none;">
                                    <span class="small fw-bold text-truncate text-dark"
                                          style="max-width: 120px;">{{ app.user.company ? app.user.company.name : 'No Company' }}</span>
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-3 p-2 rounded-4">
                                    <li><h6 class="dropdown-header text-uppercase fs-xs fw-bold text-muted px-2 py-1">Active Workspace</h6></li>
                                    {% if app.user.company %}
                                        <li><a class="dropdown-item rounded-3 active fw-medium py-2" href="{{ path('app_company') }}">{{ app.user.company.name }}</a></li>
                                        <li><hr class="dropdown-divider my-2"></li>
                                    {% endif %}
                                    <li><a class="dropdown-item rounded-3 fw-medium py-2 text-primary modern-hover" href="{{ path('app_company_new') }}"><i class="bi bi-plus-circle me-2"></i>Create New Company</a></li>
                                </ul>
                            </div>

                            <div class="dropdown dropdown-hover">
                                <a href="#" class="d-block text-decoration-none no-arrow modern-hover rounded-circle p-1 transition-all" id="profileDropdown"
                                   data-bs-toggle="dropdown" aria-expanded="false" style="outline: none; box-shadow: none;">
                                    <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center fw-bold shadow-sm"
                                         style="width: 40px; height: 40px; font-size: 15px;">
                                        {{ app.user.firstName|first|upper }}{{ app.user.lastName ? app.user.lastName|first|upper : '' }}
                                    </div>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-3 p-2 rounded-4"
                                    aria-labelledby="profileDropdown" style="min-width: 240px;">
                                    <li>
                                        <div class="px-3 py-2 mb-1">
                                            <div class="fw-bold text-dark fs-6">{{ app.user.firstName }} {{ app.user.lastName }}</div>
                                            <div class="text-muted small text-truncate">{{ app.user.email }}</div>
                                        </div>
                                    </li>
                                    <li><hr class="dropdown-divider my-1"></li>
                                    <li><a class="dropdown-item rounded-3 py-2 fw-medium text-secondary modern-hover" href="{{ path('app_account_settings') }}"><i class="bi bi-gear me-2"></i>Settings</a></li>
                                    <li><a class="dropdown-item rounded-3 py-2 fw-medium text-danger modern-hover-danger" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i>Sign out</a></li>
                                </ul>
                            </div>
                        </div>

                        {# ----------------- MOBILE ACCOUNT VIEW ----------------- #}
                        <div class="d-lg-none mt-4 w-100">
                            <div class="d-flex align-items-center px-2 mb-4">
                                <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center fw-bold me-3 shadow-sm"
                                     style="width: 54px; height: 54px; font-size: 20px;">
                                    {{ app.user.firstName|first|upper }}{{ app.user.lastName ? app.user.lastName|first|upper : '' }}
                                </div>
                                <div class="overflow-hidden">
                                    <div class="fw-bold text-dark fs-5">{{ app.user.firstName }} {{ app.user.lastName }}</div>
                                    <div class="small text-muted text-truncate">{{ app.user.email }}</div>
                                </div>
                            </div>

                            <div class="bg-light rounded-4 p-3 mb-4 border border-light">
                                <div class="d-flex align-items-center justify-content-between mb-2">
                                    <small class="text-uppercase text-muted fw-bold" style="font-size: 0.7rem;">Active Workspace</small>
                                    {% if app.user.company %}
                                        <a href="{{ path('app_company') }}" class="text-decoration-none small fw-bold text-primary">Manage</a>
                                    {% endif %}
                                </div>
                                <div class="d-flex align-items-center gap-2 mb-3">
                                    <span class="fw-bold text-dark fs-6">{{ app.user.company ? app.user.company.name : 'No Company Selected' }}</span>
                                </div>

                                <div class="d-grid gap-2">
                                    <a href="{{ path('app_company_new') }}" class="btn btn-white bg-white border shadow-sm text-start rounded-pill py-2 fw-medium text-primary">
                                        <i class="bi bi-plus-circle me-2"></i> Create New Company
                                    </a>
                                </div>
                            </div>

                            <div class="d-grid gap-2">
                                {% if not app.user.isVerified %}
                                    <a href="{{ path('app_send_email_verification') }}" class="btn btn-warning text-white fw-bold rounded-pill py-2 mb-2">
                                        <i class="bi bi-exclamation-triangle-fill me-2"></i> Verify Email
                                    </a>
                                {% endif %}

                                <a href="{{ path('app_account_settings') }}" class="btn btn-light text-start text-dark fw-medium px-4 py-3 rounded-pill mb-2">
                                    <i class="bi bi-gear me-3 text-muted"></i> Account Settings
                                </a>
                                <a href="{{ path('app_logout') }}" class="btn btn-light text-start text-danger fw-medium px-4 py-3 rounded-pill">
                                    <i class="bi bi-box-arrow-right me-3"></i> Sign out
                                </a>
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>

    </div>
</nav>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const canHover = window.matchMedia('(hover: hover) and (pointer: fine) and (min-width: 992px)');
        if (!canHover.matches) return;

        document.querySelectorAll('.dropdown-hover').forEach((dd) => {
            const toggle = dd.querySelector('[data-bs-toggle="dropdown"]');
            const menu = dd.querySelector('.dropdown-menu');
            if (!toggle || !menu) return;

            const instance = bootstrap.Dropdown.getOrCreateInstance(toggle);
            let hideTimer = null;

            const showMenu = () => {
                clearTimeout(hideTimer);
                instance.show();
                toggle.setAttribute('aria-expanded', 'true');
            };

            const hideMenu = () => {
                hideTimer = setTimeout(() => {
                    instance.hide();
                    toggle.setAttribute('aria-expanded', 'false');
                }, 300);
            };

            dd.addEventListener('mouseenter', showMenu);
            dd.addEventListener('mouseleave', hideMenu);

            toggle.addEventListener('click', (e) => {
                if (menu.classList.contains('show')) {
                    e.preventDefault();
                    e.stopPropagation();
                    instance.show();
                }
            });

            menu.addEventListener('click', (e) => {
                e.stopPropagation();
                clearTimeout(hideTimer);
            });
        });
    });
</script>
<style>
    /* Efecte moderne hover pentru butoane și link-uri */
    .transition-all {
        transition: all 0.2s ease-in-out;
    }

    .modern-hover:hover {
        background-color: rgba(0, 0, 0, 0.04);
        color: #000 !important;
    }

    .modern-hover-danger:hover {
        background-color: rgba(220, 53, 69, 0.08);
        color: #dc3545 !important;
    }

    @media (min-width: 992px) {
        /* Setări de bază pentru animație Dropdown */
        .dropdown-hover .dropdown-menu {
            display: block !important;
            opacity: 0;
            visibility: hidden;
            transform: translateY(10px);
            transition: opacity 0.25s ease-in-out, transform 0.25s ease-in-out, visibility 0.25s;
        }

        .dropdown-hover .dropdown-menu.show {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .dropdown-hover .dropdown-menu-end {
            right: 0 !important;
            left: auto !important;
        }

        .dropdown-hover .dropdown-menu::before {
            content: '';
            position: absolute;
            top: -15px;
            left: 0;
            width: 100%;
            height: 20px;
            background: transparent;
            z-index: -1;
        }
    }
</style>