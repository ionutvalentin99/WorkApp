{% extends 'base.html.twig' %}

{% block title %}

    {% if app.user %}
    Salut, {{ app.user.username }}
    {% else %}
    Welcome!
    {% endif %}
{% endblock %}

{% block body %}
    {% if app.user %}
    <p class="justify-center"> Bun venit, {{ user }}! </p>
    {% endif %}
{% endblock %}
